FROM python:latest

ENV DEBIAN_FRONTEND=noninteractive \
    PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PIP_NO_CACHE_DIR=1

ARG TARGETARCH

# hadolint ignore=DL3008
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    pkg-config \
    curl \
    ca-certificates \
    gnupg \
    git \
    unzip \
    xz-utils \
    bzip2 \
    graphviz \
    poppler-utils \
    mupdf-tools \
    shellcheck \
  && curl -fsSL https://deb.nodesource.com/setup_20.x -o /tmp/nodesource.sh \
  && bash /tmp/nodesource.sh \
  && rm -f /tmp/nodesource.sh \
  && apt-get install -y --no-install-recommends nodejs \
  && rm -rf /var/lib/apt/lists/*

RUN set -eux; \
  arch="x86_64"; \
  if [ "${TARGETARCH}" = "arm64" ]; then arch="aarch64"; fi; \
  curl -fL -o /tmp/typst.tar.xz \
    "https://github.com/typst/typst/releases/download/v0.14.2/typst-${arch}-unknown-linux-musl.tar.xz"; \
  tar -xJf /tmp/typst.tar.xz -C /tmp; \
  mv /tmp/typst-*/typst /usr/local/bin/typst; \
  chmod +x /usr/local/bin/typst; \
  rm -rf /tmp/typst*; \
  shfmt_arch="amd64"; \
  hadolint_arch="x86_64"; \
  if [ "${TARGETARCH}" = "arm64" ]; then shfmt_arch="arm64"; hadolint_arch="arm64"; fi; \
  curl -fL -o /usr/local/bin/shfmt \
    "https://github.com/mvdan/sh/releases/download/v3.8.0/shfmt_v3.8.0_linux_${shfmt_arch}"; \
  chmod +x /usr/local/bin/shfmt; \
  curl -fL -o /usr/local/bin/hadolint \
    "https://github.com/hadolint/hadolint/releases/download/v2.12.0/hadolint-Linux-${hadolint_arch}"; \
  chmod +x /usr/local/bin/hadolint

WORKDIR /app

COPY requirements.txt ./
RUN python -m pip install --no-cache-dir -r requirements.txt

RUN python -m pip install --no-cache-dir --no-deps semgrep==1.146.0 \
  && python -m playwright install --with-deps chromium

COPY . .

EXPOSE 3000 8000

RUN chmod +x /app/scripts/run_maxcov.sh /app/scripts/run_ui_tests.sh

CMD ["/app/scripts/run_ui_tests.sh"]
